import { Canvas } from '@react-three/fiber';
import { SceneContainer } from './SceneContainer'
const Portals = () => {
  return (<Canvas>
      <SceneContainer />
  </Canvas>
  )
}

export default Portals